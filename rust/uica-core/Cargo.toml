[package]
name = "uica-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
uica-model = { path = "../uica-model" }
uica-data = { path = "../uica-data" }
uica-decoder = { path = "../uica-decoder" }

[dev-dependencies]
hex = "0.4"
tempfile = "3"
